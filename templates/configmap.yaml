{{- $fullname := include "app.fullname" . }}
{{- $labels := include "app.labels" . }}
{{- range $val := .Values.configMap }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $fullname }}-{{ $val.suffix }}
  labels:
    {{- $labels | nindent 4 }}
data:
  {{- toYaml $val.data | nindent 4}}
{{- end }}